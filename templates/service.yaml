apiVersion: v1
kind: Service
metadata:
  name: {{ include "phpinfo-1.fullname" . }}
  labels:
    {{- include "phpinfo-1.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "phpinfo-1.selectorLabels" . | nindent 4 }}
